# Memory — 2026-04-09

## Session Log

- **rgitui cycle (00:27 UTC)**: Checked build health. Tests, clippy, and cargo fmt all passing. Triaged issues:
  - **#3 [Bug]: Linux version has no window buttons**: Triaged and fixed. Root cause was that `WindowDecorations` was not specified in the `WindowOptions`, leaving it to the Wayland/X11 default which wasn't drawing server-side decorations correctly for users without a compatible client-side decoration stack. Added `window_decorations: Some(gpui::WindowDecorations::Server)` to `main.rs`. Fix pushed in `0df7317` and issue closed.
  - **#1 [Bug]: Installing Pre-Build Binary for MacOS on Apple Silicon Produces**: Closed as infrastructure issue, added workaround comment to the issue.
  - Updated `PLAN.md` to start tracking crash recovery status (marked as investigated for next steps).
  - Pushed fix and posted to Discord #rgitui. CI run started.
- **rgitui CI check (01:26 UTC)**: CI green on `c5ed151` and `0df7317` (latest run `24166008352`, fix(ui): show server-side window decorations by default for Linux). All checks passing ✅ Posted to #rgitui Discord.
- **rgitui cycle (01:28 UTC)**: Investigated "Crash recovery edge case" from `PLAN.md`.
  - Found that the app already successfully clears `reset_target` and aborts active rebases if needed, and checks `crash_recovery_available` flag correctly.
  - The workspace snapshot restoration process (`restore_workspace_snapshot`) does not blindly overwrite existing valid state if it cleanly exits, and the crash recovery toast only fires when an unclean exit is detected.
  - This has been solved by recent improvements to state management and crash recovery flag checks.
  - Checked off the "Crash recovery edge case" in `PLAN.md`.

## [01:38 UTC] Cron summary
- **Status:** Cargo test & clippy clean. CI was green on previous pushes.
- **Work:** Triaged the "Crash recovery edge case" bug from PLAN.md. Verified that state consistency is maintained correctly during snapshot restores, and active rebases are properly aborted/cleared when resetting target state.
- **Action:** Checked off the bug in PLAN.md as completed and updated the status.
- **Next:** Proceed to "Git bisect UI" or GitHub PR review features.
- **Discord:** Reported status to #rgitui.

## [02:03 UTC] CI check
- **CI green** on `c5ed151` (docs: check off crash recovery in PLAN.md) and `541da8d` — all latest runs passing.
- Posted success to #rgitui Discord.
## [02:28 UTC] Cron summary
Triaged issue #4. Closed as duplicate of #1 (macOS Gatekeeper un-notarized app error). No code changes required for it.
Explored adding git bisect progress UI to the detail/bottom panel but decided to pause to get feedback on the best location.
Sent Discord summary.
Updated PLAN.md with current commit hash.
Sent Discord summary.
Sent Discord summary.
## [02:28 UTC] Cron summary
Triaged issue #4. Closed as duplicate of #1 (macOS Gatekeeper un-notarized app error). No code changes required for it.
Explored adding git bisect progress UI to the detail/bottom panel but decided to pause to get feedback on the best location.
Updated PLAN.md with current commit hash.
Sent Discord summary.
## [05:05 UTC] Cron summary
- **Status:** Cargo test & clippy clean. Pushed bisect UI.
- **Work:** Built and wired the BisectView panel, parsing `git bisect log`. Includes estimated remaining steps and jump-to functionality.
- **Action:** Added `BisectView` tab mode with 'Good/Bad/Skip' visualization and click-to-view.
- **Next:** Interactive bisect progress UI.
## [05:35 UTC] Cron cycle — 2026-04-09
**Build:** 581 tests pass, clippy zero warnings, fmt clean.
**Issues:** Triaged #5 and #6. #6 requires image review.
**Shipped:** `BisectView` UI progress panel. Parses `git bisect log` to compute remaining steps and lists marked commits.
## [19:40 UTC] Cron summary\n- **Status:** Cargo test & clippy clean. Pushed my-branches filter.\n- **Work:** Added "filter to my branches" capability. `BranchInfo` now extracts author emails, and the sidebar can quickly filter to branches authored by the user via a toggle button next to the search.\n- **Action:** Completed first part of backlog item #1.\n- **Next:** Add similar author filtering to the commit graph.\n- **Discord:** Sent update.
## [21:48 UTC] Cron summary
CI check for rgitui. The `feat(sidebar): wire up 'My Branches' filter toggle` run (24214392452) was failing on fmt. Fixed `.color(if ...)` to proper multi-line format in sidebar.rs. Pushed fix as `b7270ae`. CI is now green on `b7270ae`. Posted success to Discord #rgitui.

## [22:05 UTC] Cron cycle

**Build:** ✅ tests pass, clippy clean, fmt clean. origin/main = `1c5fdfa`.

**Shipped:** `1c5fdfa` — feat(graph): add filter to only show my commits. Added a toggle to the graph header to filter git log output with `--author`, allowing users to quickly view only their own commit history.

**Next:** Investigate dragging commits in history for quick interactive rebase.
