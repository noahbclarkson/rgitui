// Scroll list — thin wrapper around gpui::uniform_list for virtualized rendering.
// We export the GPUI types directly and provide a helper for common patterns.

pub use gpui::uniform_list;
pub use gpui::UniformListScrollHandle;
